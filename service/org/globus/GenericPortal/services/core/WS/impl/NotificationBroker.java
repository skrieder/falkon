package org.globus.GenericPortal.services.core.WS.impl;


import java.util.*;

// Declare the model
    public class NotificationBroker extends Observable {
        // The setChanged() protected method must overridden to make it public
        public synchronized void setChanged() {
            super.setChanged();
        }

        //NotificationBroker nb = new NotificationBroker();
        
    }
