export FALKON_HOME=/disks/ci-gpfs/swift/falkon/falkon
#export FALKON_HOME=${FALKON_HOME}
export JAVA_HOME=${FALKON_HOME}/jdk1.6.0_07
export ANT_HOME=${FALKON_HOME}/apache-ant-1.7.0
export GLOBUS_LOCATION=${FALKON_HOME}/container
export FALKON_SERVICE_HOME=${FALKON_HOME}/service
export FALKON_WORKER_HOME=${FALKON_HOME}/worker
export FALKON_CLIENT_HOME=${FALKON_HOME}/client
export FALKON_CLIENT_WORKLOADS_HOME=${FALKON_HOME}/workloads
export FALKON_MONITOR_HOME=${FALKON_HOME}/monitor
export FALKON_WWW_HOME=${FALKON_HOME}/webserver
export FALKON_CONFIG=${FALKON_HOME}/config
export FALKON_LOGS=${FALKON_HOME}/logs
export PLOTICUS_HOME=${FALKON_HOME}/ploticus
export GLOBUS_PATH=${GLOBUS_LOCATION}
export LD_LIBRARY_PATH=${GLOBUS_LOCATION}/lib:${LD_LIBRARY_PATH}

export GLOBUS_TCP_PORT_RANGE=50000,59999


HEAP_SIZE=1536M
STACK_SIZE=128K
export GLOBUS_OPTIONS_MISC="-Xms${HEAP_SIZE} -Xmx${HEAP_SIZE} -Xss${STACK_SIZE}"

CUR_DIR=`pwd`

cd ${FALKON_HOME}
cd ..
export FALKON_ROOT=`pwd`
cd ${CUR_DIR}

export PATH=${FALKON_HOME}/bin:${FALKON_SERVICE_HOME}:${FALKON_WORKER_HOME}:${FALKON_CLIENT_HOME}:${FALKON_MONITOR_HOME}:${FALKON_WWW_HOME}:${PLOTICUS_HOME}/src:${ANT_HOME}:${ANT_HOME}/bin:${JAVA_HOME}:${JAVA_HOME}/bin:${GLOBUS_LOCATION}:${GLOBUS_LOCATION}/bin:${PATH}

export CLASSPATH=

source ${GLOBUS_LOCATION}/etc/globus-devel-env.sh

